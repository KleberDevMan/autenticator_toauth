<div class="container" style="width: 70%; margin-top: 100px;">
  <div class="row">
    <div class="col s4 offset-s4 card">
      <h6>fim</h6>
      <%= link_to 'autenticar novamente', index_index_path %>

      <p>Resultado:</p>
      <%i = 1
        $request_questions.each do |request_question| %>
        resposta <%= i%>: <%= request_question.value %><br>
        <% i = i+1 %>
      <% end %>
    </div>
  </div>
</div>